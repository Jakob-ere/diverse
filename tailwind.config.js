/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class",
  // v4 auto-scans files; adding content isn't required, but harmless if you want:
  content: ["./src/**/*.{ts,tsx,js,jsx,mdx}"],
};
